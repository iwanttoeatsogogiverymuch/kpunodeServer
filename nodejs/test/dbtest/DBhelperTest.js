const mysql = require('promise-mysql');
const DBhelper = require('../db/DBhelper');
const assert = require('assert');

describe('DBhelper test', function() {
    it('DBhelper is Singleton Object..',function(done) {

			DBhelper.
			done();
		});
		it('getConn should return Connection..',function(done){
			done();
		});
		it('the connection is verifyed..',function(done){
			done();
		});
  }
);
